{{!-- This #if is a helper to evaluate if teh variable or expression exists --}}
{{#if data.image}}
<img class="hero img-fluid" src="{{data.image}}" alt="">
{{/if}}
<h1 class="primaryheading">{{data.title}}</h1>
<p class="abstract">{{data.abstract}}</p>
