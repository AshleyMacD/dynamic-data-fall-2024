{{!-- This #if is a helper to evaluate if teh variable or expression exists --}}
{{#if image}}
<img class="hero img-fluid" src="/images/{{image}}" alt="">
{{/if}}
<h1 class="primaryheading">{{title}}</h1>
<p class="abstract">{{abstract}}</p>
